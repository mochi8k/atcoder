function main(input) {
    const args = input.split('\n');
    const N = args[0];
    const nums = args.slice(1, args[0]);
    nums.every((n) => {
        var ns = n.split(' ');
        var t = ns[0],
            x = ns[1],
            y = ns[2];

    });
}
main(require('fs').readFileSync('/dev/stdin', 'utf8'));
